# cs61a_maps

Used Yelp review dataset to create a visualization of restaurant ratings around Berkeley in Python. Used k-means unsupervised learning algorithm to group restaurants into clusters. Used linear regression to predict the rating a user would give a restaurant.

